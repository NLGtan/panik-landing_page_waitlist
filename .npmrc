# wagmi@3 declares peerOptional typescript >=5.9.3, but this repo pins TS ~5.8.
# The override is safe (typescript is an optional peer, only used for types), and
# without this a plain `npm install` — including Vercel's — fails with ERESOLVE.
legacy-peer-deps=true
